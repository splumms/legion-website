"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
module.exports = void 0;

var _pathe = require("pathe");

var _default = {
  webpackFinal(config, extras) {
    const buildDir = extras.nuxt.options.buildDir;
    config.resolve.alias = { ...extras.nuxtWebpackConfig.resolve.alias,
      ...config.resolve.alias,
      "~storybook": (0, _pathe.resolve)(__dirname, "../storybook"),
      ...extras.nuxtBuilder.plugins.reduce((map, plugin) => ({ ...map,
        [plugin.name]: (0, _pathe.resolve)(buildDir, plugin.src)
      }), {})
    };
    return config;
  }

};
module.exports = _default;